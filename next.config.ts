import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "cms.amraj.in", "sachdevamedline.com", "cms.sachdevamedline.com"],
  },
  /* config options here */
  reactStrictMode: true,
};

export default nextConfig;
